{
  "meta": { "source": "docs", "recordedAt": "2026-09-23", "notes": "GOOGLE_CLIENT_SECRET mal rotado en el vault. Es culpa nuestra: transitorio, no needs_reauth (oauth/errors.ts, OUR_FAULT_CODES)." },
  "request": { "method": "POST", "urlPattern": "^https://oauth2\\.googleapis\\.com/token$", "body": { "grant_type": "refresh_token" } },
  "response": { "status": 401, "body": { "error": "invalid_client", "error_description": "The OAuth client was not found." } }
}
